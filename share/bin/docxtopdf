#!/usr/bin/env bash

if ! command -v libreoffice &>/dev/null; then
        exit 1
fi

for file in *.docx; do
        [ -e "$file" ] || {
                exit 0
        }

        libreoffice --headless --convert-to pdf "$file" --outdir .
done
